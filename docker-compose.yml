version: "3.8"
services:
  torrserver:
    image: yourname/torrserver:latest
    container_name: torrserver
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ./config:/config
      - ./torrents:/torrents
